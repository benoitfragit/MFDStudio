# Picture Assets

This folder stores bitmap assets referenced by JSON `image` primitives.

The shipped demo page uses `mfdstudio_badge.png` from this folder through:

- `assets/reticles/picture_demo_badge.json`
- `assets/pages/picture_demo.json`

Image primitive paths are resolved relative to the page or reticle JSON file
that references them.
